export interface Order {
    id: string;
    isPaid: boolean;
    order_amount: string;
    order_appointment_date: string;
    order_appointment_time: string;
    order_contact: string;
    order_date: string;
    order_email: string;
    order_payment_id: string;
    order_user_name: string;
}
